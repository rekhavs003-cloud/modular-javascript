/*
    The initial test is completely failing (RED). 
    Write the absolute minimum application code below to make it PASS (GREEN).
*/
export function reduceDamage(rawDamage, armorValue) {
	return rawDamage - armorValue;
}
